{
  "name": "glucosnap-utils",
  "version": "1.0.0",
  "description": "Utility functions for GlucoSnap",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-jwt-verify": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
